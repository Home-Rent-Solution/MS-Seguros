spring:
  application:
    name: MS-Seguros

  datasource:
    url: jdbc:mysql://localhost:3306/ms_seguros
    username: root
    password: Duoc.2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8089